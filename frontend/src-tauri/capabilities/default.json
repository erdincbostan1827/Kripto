{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimal local shell capability; no exchange-secret, filesystem, shell, or process permissions.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
