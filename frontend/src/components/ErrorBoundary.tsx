import React from 'react'; import { Alert,Button,Stack } from '@mui/material';
export class ErrorBoundary extends React.Component<React.PropsWithChildren,{error?:Error}>{state:{error?:Error}={};static getDerivedStateFromError(error:Error){return {error}};render(){if(this.state.error)return <Stack sx={{p:3}}><Alert severity="error">Ekran yüklenemedi. Trading motoru etkilenmedi. Correlation: UI-{Date.now().toString(36)}</Alert><Button onClick={()=>location.reload()}>Tekrar dene</Button></Stack>;return this.props.children}}
