import {Alert,Box,Chip,Stack,Typography} from '@mui/material'; import {ModeBadge} from './ModeBadge'; import {dataHealthPresentation,type DataHealth} from '../ux/dataHealth';
const DEFAULT_MARKET_DATA_LABEL='Market Data: Fresh';
export function StatusStrip({dataHealth={stale:false,needsResync:false,ageMs:0}}:{dataHealth?:DataHealth}){const now=new Date();const health=dataHealthPresentation(dataHealth);const marketDataLabel=health.severity==='success'?DEFAULT_MARKET_DATA_LABEL:health.label;return <Box role="status" aria-live="polite" sx={{p:1.5,borderBottom:1,borderColor:'divider',position:'sticky',top:0,zIndex:20,bgcolor:'background.paper'}}><Stack spacing={1}><Stack direction="row" spacing={1} sx={{alignItems:'center',flexWrap:'wrap'}}><ModeBadge mode="PAPER"/><Chip label="Exchange: Mock / Ready" color="success" variant="outlined"/><Chip label={marketDataLabel} color={health.severity==='success'?'success':health.severity==='warning'?'warning':'error'} variant="outlined"/><Chip label="Engine: Safe" color="success" variant="outlined"/><Chip label="Risk: Normal" color="success" variant="outlined"/><Chip label={`Server/Local: ${now.toLocaleTimeString()}`} variant="outlined"/><Typography variant="caption" sx={{ml:{sm:'auto'}}}>LIVE varsayılan olarak kapalıdır</Typography></Stack>{health.blocking&&<Alert severity={health.severity==='error'?'error':'warning'} role="alert">{health.label}. Yeni risk artırıcı kararlar veri sağlığı düzelene kadar bloklanır.</Alert>}</Stack></Box>}
